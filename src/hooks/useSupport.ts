export function useSupport() {
    // 處理分類
    const handleCategoryClick = (category: string) => { };
    // 處理搜尋
    const handleSearch = (search: string) => { };

    return {
        handleCategoryClick,
        handleSearch,
    };
}